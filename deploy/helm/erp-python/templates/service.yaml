apiVersion: v1
kind: Service
metadata:
  name: {{ include "erp-python.fullname" . }}
  labels:
    {{- include "erp-python.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "erp-python.selectorLabels" . | nindent 4 }}
